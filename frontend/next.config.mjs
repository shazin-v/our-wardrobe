/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add the domain from which the images are being fetched
      },
};

export default nextConfig;
